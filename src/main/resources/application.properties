# Fixed port required so viewer clients can connect via tournament.server.url; tests use RANDOM_PORT and are unaffected
server.port=8080
tournament.server.url=http://localhost:8080
tournament.id=ipd-01
participant.name=TitForTat
